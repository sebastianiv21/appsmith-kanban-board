SELECT * FROM public."oracle_support_ticket"
{{DataBoardFilters.getFilters()}}
AND status != 'Resolved' OR status != 'Ignore'
AND slack_thread IS NOT NULL OR slack_thread != ''
ORDER BY updated_at DESC
LIMIT 50;